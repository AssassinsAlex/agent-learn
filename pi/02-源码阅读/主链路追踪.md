# 主链路追踪

| 步骤 | 源码位置 | 调用/事件 | 状态变化 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `packages/coding-agent/src/main.ts` | CLI 解析并选择 interactive/print/rpc | 创建 `AgentSessionRuntime` | `main()` 在约 615-741 行组装 services/session |
| 2 | `packages/coding-agent/src/core/agent-session.ts` | `AgentSession.prompt()` | prompt 进入 session/context | 共享外壳，所有 mode 使用 |
| 3 | `packages/agent/src/agent-loop.ts:154-280` | `runLoop()` -> `streamAssistantResponse()` | `agent_start` -> `turn_start` -> streaming events | 通过 `pi-ai` 的 `streamFunction` 请求模型 |
| 4 | `packages/agent/src/agent-loop.ts:410-511` | `executeToolCalls()` | 发出 tool_execution_start/end | 支持 sequential/parallel；截断参数不会直接执行 |
| 5 | `packages/agent/src/agent-loop.ts:213-228` | 追加 `ToolResultMessage` | context 与 newMessages 同步增长 | 之后可再次进入模型 turn |
| 6 | `packages/agent/src/agent-loop.ts:223-273` | `turn_end` -> `prepareNextTurn` -> `agent_end` | Agent 结束且事件监听器收束 | steering/follow-up 可能让外层循环继续 |
