# 事件与数据流

## 需要确认的事件链

```text
用户输入 -> 会话消息 -> 模型请求 -> 流式事件 -> UI 更新
                                  -> 工具调用 -> 工具结果 -> 会话消息
```

| 数据/事件 | 创建位置 | 消费位置 | 持久化 | 备注 |
| --- | --- | --- | --- | --- |
| 待定位 | 待定位 | 待定位 | 待确认 | 待填写 |
