# 问题清单

| 编号 | 问题 | 影响模块 | 证据/线索 | 状态 |
| --- | --- | --- | --- | --- |
| PI-001 | 此次学习对应的 Pi 官方仓库、版本和本地源码路径是什么？ | 全部 | `0.80.10` / `864b35c`；见[项目档案](../00-总览/项目档案.md) | 已解决 |
| PI-002 | Agent loop 的真实入口和停止条件分别是什么？ | Agent 循环 | 见[Agent 循环第一讲](../01-核心模块/03-Agent循环-第一讲.md) | 已解决（第一版） |
| PI-003 | 工具执行的权限模型和确认点在哪里？ | 工具/安全 | 工具主链已记录；project trust 与 extension 权限待补 | 部分解决 |
| PI-004 | 会话如何存储、恢复和压缩？ | 会话/上下文 | 通用 Harness 已记录；CLI JSONL 恢复与自动策略待补 | 部分解决 |
| PI-005 | Pi CLI 在何处装配 settings、models、auth、extensions 和 `AgentSession`？ | 应用运行时 | `agent-session-runtime.ts`、`agent-session-services.ts` | P0 待开始 |
| PI-006 | 不同 LLM provider 的 request/message/stream 如何归一化，deferred tools 的准确语义是什么？ | `pi-ai` | `packages/ai/src/types.ts`、`api/*` | P0 待开始 |
| PI-007 | project trust 实际阻止哪些项目资源，哪些副作用仍不被沙箱限制？ | 扩展/安全 | `trust-manager.ts`、`project-trust.ts` | P0 待开始 |
| PI-008 | 同一 JSONL session 分支与 `parentSession` 跨文件 fork 的语义和恢复差异是什么？ | CLI 会话 | `session-manager.ts` | P1 待开始 |
| PI-009 | JSONL RPC 如何把 command、event 和 extension UI 转换成宿主协议？ | 控制面 | `modes/rpc/rpc-mode.ts` | P1 待开始 |
| PI-010 | Pi 默认是否具备“轨迹抽取 -> 长期经验库 -> 检索注入”的闭环？若无，最小 extension 如何实现并评测？ | 记忆/算法 | 目前仅见 session persistence、Skills 与 extension；见[覆盖审查](../00-总览/全项目学习覆盖审查.md) | 已确认默认无；P0 扩展课题 |

## 阶段复盘模板

### 本阶段已确认

待填写。

### 仍不确定

待填写。

### 下一阶段入口

待填写。
